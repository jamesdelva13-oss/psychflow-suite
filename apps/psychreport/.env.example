# apps/psychreport environment — copy to .env.local and fill in. Never commit values.
#
# Same Supabase project as apps/intake (CF-8: one canonical case model, one
# auth/RLS posture — PsychReport consumes the shared case, it does not host a
# copy). Synthetic data only per data-posture §10.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
